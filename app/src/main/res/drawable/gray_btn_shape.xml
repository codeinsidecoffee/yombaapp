<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <gradient
        android:endColor="@color/btn_grey"
        android:centerColor="@color/btn_grey"
        android:startColor="@color/btn_grey"
        android:angle="270" />
    <corners
        android:radius="30dp" />

</shape>